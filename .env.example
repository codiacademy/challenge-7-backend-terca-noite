# Database
DATABASE_URL=mysql://USER:PASSWORD@HOST@localhost:3306/DBNAME?schema=public

# JWT
JWT_SECRET=change-this-secret-to-a-random-secure-value-in-production

# Environment
NODE_ENV=development

# Ports (for local development without Docker)
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Optional: PostgreSQL (for local development)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=codi_db

